<!DOCTYPE html>
<html>

<%= erb :_head, locals: { copy: @copy } %>

<body>
    <div id="horz">
        <%= erb :_nav %>
        <div id="main">
            <% content.each do |document| %>
                <%= erb :_fragment, locals: { document: document } %>
            <% end %>
        </div>
    </div>
</body>

</html>