<!DOCTYPE html>
<html>

<%= erb :_head, locals: { copy: @copy } %>

<body>
    <div id="horz">
        <%= erb :_nav %>
        <div id="main">
            <img style="max-width: 100%;" src="<%= @document.uri %>">
        </div>
    </div>
</body>

</html>
