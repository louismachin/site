<!DOCTYPE html>
<html>

<%= erb :_head, locals: { copy: @copy } %>

<body>
    <div id="horz">
        <%= erb :_nav %>
        <div id="main">
            <%= erb :_toolbox, locals: { links: { "Edit" => document.edit_uri, }} %>
            <%= erb :_fragment, locals: { document: @document } %>
        </div>
    </div>
</body>

</html>